from core.facade import get_agent_item_tables

#  def agent_has_access_to_this_item_category(agent, item_category):
    #  item_tables = get_agent_item_tables(agent)
    #  if item_category.item_table not in item_tables:
        #  return False
    #  else: 
        #  return True
